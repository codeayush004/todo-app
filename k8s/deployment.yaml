apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-backend
  labels:
    app: todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo
  template:
    metadata:
      labels:
        app: todo
    spec:
      containers:
      - name: todo
        image: todoacrffe0a9.azurecr.io/todo-app:latest
        imagePullPolicy: Always           
        ports:
        - containerPort: 8000
        - containerPort: 8501
